{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "yarn install --frozen-lockfile",
        "buildCommand": "yarn build"
      }
    }
  ],
  "node": "20.x",
  "features": {
    "python": true
  }
}
